

spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: public
        extension-configs:
          - dataId: nacos-demo-ext.yml
            group: DEFAULT_GROUP
            refresh: true